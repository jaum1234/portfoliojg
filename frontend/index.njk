<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Portfólio de Vídeos</title>
    <link rel="stylesheet" href="styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;800&display=swap" rel="stylesheet">
</head>
<body>
    <header>
        <div class="container">
            <nav>
                <div class="language-toggle">
                    <button id="pt-btn" class="active">PT</button>
                    <button id="en-btn">EN</button>
                </div>
                <div class="theme-toggle">
                    <button id="theme-toggle-btn">
                        <i class="fas fa-moon"></i>
                    </button>
                </div>
            </nav>
        </div>
    </header>

    <main>
        <section class="hero">
            <div class="container">
                <h1 class="lang-pt">Portfólio de Vídeos Profissionais</h1>
                <h1 class="lang-en">Professional Video Portfolio</h1>
            </div>
        </section>

        <section class="categories">
            <div class="container">
                <h2 class="lang-pt">Categorias</h2>
                <h2 class="lang-en">Categories</h2>
                <div class="category-filters">
                    <button class="category-btn active" data-category="all">
                        <span class="lang-pt">Todos</span>
                        <span class="lang-en">All</span>
                    </button>
                    {% for category in api.categorias %}
                        <button class="category-btn" data-category="{{ category.id }}">
                            <span class="lang-pt">{{ category.nome['pt-br'] }}</span>
                            <span class="lang-en">{{ category.nome['en-us'] }}</span>
                        </button>
                    {% endfor %}
                </div>
            </div>
        </section>

        <section class="videos">
            <div class="container">
                <div class="video-grid" id="video-grid">
                    {% for video in api.videos %}
                        <div class="video-card" data-category="{{ video.categoriaId }}" data-id="{{ video.id }}">
                            <div class="video-thumbnail">
                                <img src="{{ video.url_miniatura }}" alt="{{ video.titulo['pt-br'] }}">
                                <div class="play-icon">
                                    <i class="fas fa-play"></i>
                                </div>
                            </div>
                            <div class="video-info">
                                <h3 class="lang-pt">{{ video.titulo['pt-br'] }}</h3>
                                <p class="lang-pt">{{ video.descricao['pt-br'] | truncate(100, true, '...') }}</p>
                                <h3 class="lang-en">{{ video.titulo['en-us'] }}</h3>
                                <p class="lang-en">{{ video.descricao['en-us'] | truncate(100, true, '...') }}</p>
                            </div>
                        </div>
                        <div class="modal" data-modal data-video-id="{{ video.id }}">
                            <div class="modal-content">
                                <span class="close-modal">&times;</span>
                                <div class="video-container" id="modal-video-container-{{ video.id }}">
                                    <iframe 
                                        src="{{ video.url_video }}" 
                                        title="{{ video.titulo['pt-br'] }}" 
                                        frameborder="0" 
                                        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" 
                                        class="lang-pt"
                                        allowfullscreen>
                                    </iframe>
                                    <iframe 
                                        src="{{ video.url_video }}" 
                                        title="{{ video.titulo['en-us'] }}" 
                                        frameborder="0" 
                                        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" 
                                        class="lang-en"
                                        allowfullscreen>
                                    </iframe>
                                </div>
                                <div class="video-info">
                                    <h3 class="lang-pt" id="modal-video-title">{{ video.titulo['pt-br'] }}</h3>
                                    <p class="lang-pt" id="modal-video-description">{{ video.descricao['pt-br'] }}</p>
                                    <h3 class="lang-us" id="modal-video-title">{{ video.titulo['en-us'] }}</h3>
                                    <p class="lang-us" id="modal-video-description">{{ video.descricao['en-us'] }}</p>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </section>
    </main>

    <footer>
        <div class="container">
            <p>&copy; 2025</p>
            <div class="social-links">
                <a href="https://www.instagram.com/ojaaopedro/" target="_blank"><i class="fab fa-instagram"></i></a>
                <a href="https://www.youtube.com/@jgwsh" target="_blank"><i class="fab fa-youtube"></i></a>
            </div>
        </div>
    </footer>

    <script src="main.js"></script>
</body>
</html>
